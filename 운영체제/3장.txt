fork() 쓸 떄 wait() 없으면 부모 프로세스가 먼저 실행되는 듯
wait()실행 시 부모 프로세스는 wait queue로 감
보통 부모 프로세스가 먼저 실행되지만 항상 부모-자식 순서로 실행된다는 보장은 없음

3.1 value = 5

3.2 8개

3.3 태스크를 얼마만큼 어느정도로 나눠서 처리 할지(분할)
     각 분리된 태스크들을 각 여러 프로세서에 어떻게 배정해서 처리할지(스케줄링)
     각 프로세서에 공유된 데이터가 임의의 한 프로세서에 의해 변경되지 않도록 해야함(동기화)

3.4

3.5 공유 메모리 세그먼트만 부모 간에 공유되고 
     스택, 힙의 복사본은 새로 생성된 프로세스를 위해 만들어짐

3.6
3.7
